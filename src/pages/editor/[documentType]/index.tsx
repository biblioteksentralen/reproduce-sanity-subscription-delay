// The document type landing page shows only the menu, taken care of by the layout,
// so this component does not have to render anything
export default function DocumentTypeLandingPage() {
  return null;
}
